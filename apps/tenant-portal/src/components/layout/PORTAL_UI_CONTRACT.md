Every page uses PageShell

Use portal form primitives (TextInput, TextArea) unless @superapp/ui provides equivalents

No raw brand colors in pages (brand color must come from CSS variables)

Branding feature is gated by permission whitelabel.branding