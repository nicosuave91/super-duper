# =========================================================
# Tenant Portal (Vite) .env.example
# =========================================================
VITE_APP_ENV=development

# Where the API is reachable from the browser
VITE_API_BASE_URL=http://localhost:3002

# API route path(s) used by the UI (keep stable)
VITE_TENANT_LEADS_PATH=/api/tenant/leads

# =========================================================
# Auth0 SPA (Tenant Portal)
# =========================================================
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id
VITE_AUTH0_AUDIENCE=https://api.superapp.com

# Tenant portal callback
VITE_AUTH0_REDIRECT_URI=http://localhost:5174/callback

# Optional dev helpers
VITE_DEV_AUTO_AUTH=true
VITE_SHOW_DEBUG=true
