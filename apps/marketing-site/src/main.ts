import { createApp } from 'vue'
import App from './App.vue'; // Ensure this is correct and `.vue` files are handled
import "@package/config/tokens/colors.css";
import './style.css'

createApp(App).mount('#app')
